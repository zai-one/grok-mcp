{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "grok-agent-event.v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "sequence", "kind", "at", "payload"],
  "properties": {
    "schema_version": {"const": "grok-agent-event.v1"},
    "sequence": {"type": "integer", "minimum": 0},
    "kind": {"type": "string", "maxLength": 128},
    "at": {"type": "string"},
    "payload": {"type": "object"}
  }
}
