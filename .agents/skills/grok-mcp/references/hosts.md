# Hosts

Claude Code / Desktop / Cursor / Codex / other Agent Skills paths under `.claude|.codex|.agents/skills/grok-mcp/`.

**All hosts:** execute `plan` from `grok_agent_session_begin`; do not re-plan in prose; keep tool results compact; never paste full references into user chat.
