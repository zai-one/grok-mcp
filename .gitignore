__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/
venv/

# Stray run artifacts from other tools sharing this checkout
outputs/
*.log

# grok-mcp worktrees for delegated jobs
.grok/

# Local host config: enabled plugins are personal, not project state
.claude/settings.json

# Soak evidence is a run artifact: compared locally, not kept in history
Service/Audits/soak-*.json
