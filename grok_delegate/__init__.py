"""Dev-only grok_delegate tooling package.

Directory name uses a hyphen for repo layout; import modules via
``sys.path`` insertion (see server.py / test_grok_delegate.py) or run
``python tools/grok-delegate/server.py``.

Not a product surface. Does not extend tools/mcp admin-bridge trust.
"""
